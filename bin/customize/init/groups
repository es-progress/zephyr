#!/usr/bin/env bash
#################
## ES-Ubuntu   ##
##             ##
## Config      ##
## User groups ##
#################

# Strict mode
set -euo pipefail
IFS=$'\n\t'

source "${DIR_ESUBUNTU}/bootstrap.sh"
check-not-root || exit 1

print-header "Adding user to groups..."
groups=$(read-file-cfg "${ES_CUSTOMIZE_CFG}/init/groups")
while read -r group_name; do
    sudo adduser "${USER}" "${group_name}"
done <<< "${groups}"
print-finish

exit 0
