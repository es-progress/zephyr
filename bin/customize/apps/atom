#!/usr/bin/env bash
###############
## ES-Ubuntu ##
##           ##
## Config    ##
## Atom      ##
###############

###############
## FUNCTIONS ##
###############

## Install Atom packages
##
## @param   $1  Global/Local
## @param   $2  Config file
############################
_install-packages() {
    local selector="${1:?"Selector missing"}"
    local cfg_file="${2:?"Config file missing"}"

    print-header "Install Atom packages [${selector}]..."
    for package in $(cfg-read "${cfg_file}"); do
        apm install "${package}"
    done
    print-finish
}

##################
## SCRIPT START ##
##################

# Strict mode
set -euo pipefail
IFS=$'\n\t'

source "${PROJECT_ROOT}/bootstrap.sh"
check-not-root || exit 1

profile="${1:?"Profile missing"}"

print-header "Add Atom GPG key..."
curl -s --location --url https://packagecloud.io/AtomEditor/atom/gpgkey | sudo apt-key add -
print-finish

print-header "Add Atom repo..."
sudo add-apt-repository --yes "deb [arch=amd64] https://packagecloud.io/AtomEditor/atom/any/ any main"
print-finish

print-header "Install Atom..."
sudo apt-get install --yes --no-install-recommends atom
print-finish

if file=$(cfg-get "${profile}" apps/atom/packages.global); then
    _install-packages global "${file}"
fi
if file=$(cfg-get "${profile}" apps/atom/packages.local); then
    _install-packages local "${file}"
fi

if file=$(cfg-get "${profile}" apps/atom/config.cson); then
    print-status "Copy Atom config file..."
    cp "${file}" ~/.atom
    print-finish
fi

exit 0
