#!/usr/bin/env bash
###############
## ES-Ubuntu ##
##           ##
## Config    ##
## git       ##
###############

# Strict mode
set -euo pipefail
IFS=$'\n\t'

source "${PROJECT_ROOT}/bootstrap.sh"
check-not-root || exit 1

print-status "Global git config..."
configs=$(cfg-read "${ES_CUSTOMIZE_CFG}/git/configs")
for line in ${configs}; do
    IFS=$' \t' read -r cfg_loc cfg_name cfg_value <<< "${line}"
    if [[ "${cfg_loc}" == "system" ]]; then
        sudo git config "--${cfg_loc}" "${cfg_name}" "${cfg_value}"
    else
        git config "--${cfg_loc}" "${cfg_name}" "${cfg_value}"
    fi
done
print-finish

if [[ -f "${ES_CFG_LOCAL}/config/customize/git/configs" ]]; then
    print-status "Local git config..."
    configs=$(cfg-read "${ES_CFG_LOCAL}/config/customize/git/configs")
    for line in ${configs}; do
        IFS=$' \t' read -r cfg_loc cfg_name cfg_value <<< "${line}"
        if [[ "${cfg_loc}" == "system" ]]; then
            sudo git config "--${cfg_loc}" "${cfg_name}" "${cfg_value}"
        else
            git config "--${cfg_loc}" "${cfg_name}" "${cfg_value}"
        fi
    done
    print-finish
fi

exit 0
