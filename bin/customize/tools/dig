#!/usr/bin/env bash
###############
## ES-Ubuntu ##
##           ##
## Config    ##
## dig       ##
###############

# Strict mode
set -eufo pipefail
IFS=$'\n\t'

source "${PROJECT_ROOT}/bootstrap.sh"
check-not-root || exit 1

profile="${1:?"Profile missing"}"
if file=$(cfg-get "${profile}" tools/digrc); then
    print-status "Config dig..."
    cp --no-preserve=mode,ownership "${file}" ~/.digrc
    print-finish
fi

exit 0
